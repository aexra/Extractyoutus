<Page
    x:Class="Extractyoutus.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Extractyoutus.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" RowSpacing="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBox x:Name="URIBox" Grid.Row="0" PlaceholderText="Video URL" Paste="URIBox_Paste" KeyDown="URIBox_KeyDown"/>

        <Border Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="5">
            <ListView ItemsSource="{x:Bind Downloads}" SelectionMode="None"/>
        </Border>
        
    </Grid>
</Page>
